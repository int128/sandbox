name: sandbox

on:
  pull_request:
    paths:
      - .github/workflows/sandbox.yaml

jobs:
  do:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: ps axfu
      - run: sudo pkill -INT Runner.Listener
      - run: sleep 60
