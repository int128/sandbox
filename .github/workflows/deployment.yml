name: deployment

on:
  pull_request:
    branches: master

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/github@v6
        with:
          script: |
            core.info(JSON.stringify(context, undefined, 2))
