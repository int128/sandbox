name: release

on:
  workflow_dispatch:
    inputs:
      base:
        type: choice
        description: base branch
        options: 
          - production

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - run: gh pr create --base ${{ github.event.inputs.base }} --head master --title production --draft
        env:
          GITHUB_TOKEN: ${{ github.token }}
