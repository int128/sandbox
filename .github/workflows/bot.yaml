name: bot

on:
  pull_request:
    branches:
      - production
  pull_request_review:
    types:
      - created

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v5
        with:
          script: |
            const { base, head } = context.payload.pull_request
            await github.rest.checks.create({
              ...context.repo,
              name: 'fast-forward-merge-bot',
              head_sha: head.sha,
              conclusion: 'failure',
              output: {
                title: 'Require approval with comment `/merge`',
                summary: 'You need to approve this with a comment `/merge`',
              },
            })

  merge:
    if: |
      github.event_name == 'pull_request_review' &&
      github.event.review.state == 'APPROVED' &&
      github.event.review.body == '/merge'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v5
        with:
          script: |
            const { base, head } = context.payload.pull_request
            await github.rest.checks.create({
              ...context.repo,
              name: 'fast-forward-merge-bot',
              head_sha: head.sha,
              conclusion: 'success',
            })
      - uses: actions/github-script@v5
        with:
          script: |
            const { base, head } = context.payload.pull_request
            await github.rest.git.updateRef({
              ...context.repo,
              ref: `heads/${base.ref}`,
              sha: head.sha,
            })
      - if: failure()
        uses: actions/github-script@v5
        with:
          script: |
            const { base, head } = context.payload.pull_request
            await github.rest.checks.create({
              ...context.repo,
              name: 'fast-forward-merge-bot',
              head_sha: head.sha,
              conclusion: 'failure',
            })
